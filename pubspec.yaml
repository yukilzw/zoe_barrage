name: barrage_video
description: dy barrage video demo.
version: 1.0.0

environment:
  sdk: ">=2.8.0 <3.0.0"
  flutter: ">=1.10.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

  video_player: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
  - assets/phuthon.mp4
